# 운영(Docker/fly) 배포용 고정 버전 — 재현성 보장.
# 검증 완료 조합(58 단위테스트 통과). 업그레이드는 의도적으로, 테스트 후.
# (pyproject.toml은 라이브러리 메타라 하한 범위를 유지 — 역할 분리)
fastmcp==3.3.1
PublicDataReader==1.1.1.post2
pandas==2.2.3
requests==2.32.3
